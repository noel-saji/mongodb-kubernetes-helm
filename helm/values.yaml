namespace: mongo

mongodb:
  image: mongo
  replicas: 2
  port: 27017
  credentials:
    # base64-encoded values (admin / password)
    username: YWRtaW4=
    password: cGFzc3dvcmQ=
  storage:
    size: 1Gi
    accessMode: ReadWriteOnce
    volumeName: ""

mongoExpress:
  image: mongo-express
  replicas: 1
  port: 8081
  nodePort: 30000
